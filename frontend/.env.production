# IMPORTANTE: reemplazar este valor con la URL real del backend antes de compilar para producción.
# Ejemplo: REACT_APP_API_URL=https://api.almasoft.com
# Esta variable se hornea en el bundle durante "npm run build" — no es configurable en tiempo de ejecución.
REACT_APP_API_URL=https://REEMPLAZAR_CON_URL_DE_PRODUCCION
